/*#include "navmesh_generator.h"

void NavmeshGenerator::Generate(MeshInstance3D *_mesh) {
	print_line("generating mesh");

	geometry.AddMesh(_mesh);

	areas = MeshDecomposer::DecomposeMeshes( geometry.GetMeshes() );
	
	print_line(areas.size());
}
*/